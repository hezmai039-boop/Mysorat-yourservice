# ميسوور — سوق مزايدة الخدمات الحكومية | نشر على Replit

**كل شيء مبني في هذا الكود.** لا تعد كتابة أي جزء — المطلوب إعداد ونشر فقط.

المنتج: سوق مزايدة بنمط inDrive لثلاث فئات: **خدمات حكومية، تأسيس أعمال،
تراخيص** (15 خدمة مبذورة). العميل ينشر طلبه بميزانيته من «ابدأ الطلب»
(بلا أي اعتماد على مفتاح ذكاء اصطناعي)، مقدمو الخدمة يسجلون بأنفسهم
وتعتمدهم الإدارة، يزايدون من `/market`، العميل يقارن ويقبل من
`/operations/:id/bids` فتُقفل الصفقة بعمولة منصة 10% تُخصم من مقدم الخدمة،
ثم دفع ← مستندات ← تنفيذ ← تقييم. الواجهة داكنة بهوية inDrive (ليموني
`#C1F11D`) بقائمة جانبية: الرئيسية، طلباتي، مقدم خدمة، حسابي، الإدارة.

## الإعداد (Replit)

1. قاعدة PostgreSQL من Tools → توفر `DATABASE_URL`.
2. Secrets: `DATABASE_URL`, `JWT_SECRET`, `BOOTSTRAP_SECRET`,
   `OWNER_EMAIL`, `OWNER_PASSWORD`, `CORS_ORIGIN` (رابط الواجهة)،
   وللواجهة `VITE_API_BASE_URL` (رابط الخادم). كل ما عداها اختياري.
3. الخادم: `cd backend && npm install && npx prisma migrate deploy && npm run seed && npm run dev` (منفذ 4000).
4. الواجهة: `cd frontend && npm install && npm run build` ثم استضافة `dist`
   كنشر Static (الخادم على Reserved VM لا autoscale-to-zero).
5. افتح مرة واحدة: `/api/bootstrap?secret=…` ثم تأكد من
   `/api/bootstrap/health-check?secret=…` → `healthy: true`.

## سكربت القبول (تعريف «تم»)

1. سجّل عميلاً (فرد). سجّل حساباً ثانياً واضغط «انضم كمقدم خدمة» بتخصص.
2. من حساب المالك → الإدارة → «مقدمو خدمة بانتظار الاعتماد» → اعتماد.
3. العميل: ابدأ الطلب → «إصدار سجل تجاري» → وصف → ميزانية 300 → نشر.
4. مقدم الخدمة يراه في `/market` خلال 5 ثوانٍ (مع صافيه بعد 10% = 270)
   ويقدم عرضاً 350 / 4 أيام.
5. العميل يرى البطاقة حياً في شاشة العروض ويقبلها → تُقفل الصفقة،
   يصل إشعار «قُبل عرضك — صافيك 315»، وعروض غيره تنتهي (EXPIRED).
6. دفع (محاكاة) → مستندات → خطوات → إنجاز → تقييم متبادل.
7. لوحة الإدارة تعرض العملية وسجل التدقيق الكامل للعروض.

## قواعد لا تُكسر عند أي تعديل لاحق

- `backend/prisma/migrations/` append-only؛ أي ترحيل جديد idempotent
  ويُعكس في `COLUMN_SAFETY_NET` و`columnChecks` داخل `bootstrap.ts`.
- ملفات الترجمة add-only مع تحقق JSON بعد كل تعديل.
- المنصة لا تطلب ولا تخزن بيانات دخول أبشر/نفاذ إطلاقاً — قرار نظامي ثابت.
- بعد أي تعديل: `npx tsc --noEmit` في backend و`npm run build` في frontend.
