services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mysorat
      POSTGRES_PASSWORD: mysorat
      POSTGRES_DB: mysorat
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
